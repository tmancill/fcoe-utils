We don't ship upstream's init that is fedora/suse specific
Index: fcoe-utils/Makefile.am
===================================================================
--- fcoe-utils.orig/Makefile.am	2014-02-20 21:05:21.275801458 +0530
+++ fcoe-utils/Makefile.am	2014-02-20 21:05:53.247802744 +0530
@@ -55,18 +55,6 @@
 	contrib/fcc.sh contrib/fcoe-setup.sh contrib/fcoe_edd.sh \
 	debug/dcbcheck.sh debug/fcoedump.sh
 
-CLEANFILES = etc/initd/fcoe
-etc/initd/fcoe:
-	mkdir -p etc/initd
-	if [ -f /etc/fedora-release ] || [ -f /etc/redhat-release ]; then \
-		ln -s ${abs_srcdir}/etc/initd/initd.fedora $@; \
-	else \
-		ln -s ${abs_srcdir}/etc/initd/initd.suse $@; \
-	fi
-
-init_ddir = ${sysconfdir}/init.d
-init_d_SCRIPTS = etc/initd/fcoe
-
 dist_noinst_DATA = README COPYING INSTALL fcoe-utils.spec etc/config
 
 BASH_COMPLETION_DIR=$(DESTDIR)/etc/bash_completion.d/
